# limonitor

BLE battery monitor for LiTime/JBD LiFePO4 packs. HTTP API, TUI, SQLite logging.

```
mkdir build && cd build
cmake ..
make
```

pre-commit: `pip install -r requirements-dev.txt && pre-commit install`
